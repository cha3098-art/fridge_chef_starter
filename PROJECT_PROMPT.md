# 냉장고 셰프 (Fridge Chef) — 프로젝트 요약 프롬프트

이 문서는 지금까지 설계·완성한 핵심 내용을 한눈에 파악하기 위한 요약입니다. 새 세션을 시작할 때 이 파일 내용을 그대로 붙여넣으면 프로젝트 맥락을 빠르게 전달할 수 있습니다.

## 앱 정체성
- **냉장고 셰프** — 냉장고 속 재료로 레시피를 추천하고, 친구와 요리 대결(배틀)을 벌이고, 요리 자랑/랭킹으로 커뮤니티를 만드는 Flutter 앱
- 기술 스택: **Flutter** (Dart) + **Supabase** (Auth/Postgres/Storage/Realtime/Edge Functions), Firebase Cloud Messaging(푸시)
- 저장소: `C:\dev\fridge_chef_starter` (로컬 전용, GitHub 원격 없음 — 이 세션에서는 원격 push 안 함)
- Supabase 프로젝트 ref: `opqmulhoxidtblpuerii` (region ap-northeast-2), CLI로 직접 DB 쿼리·마이그레이션·Edge Function 배포 가능한 상태

## 핵심 기능 현황 (완성됨)
| 기능 | 상태 |
|---|---|
| 회원가입/로그인/로그아웃 | Supabase Auth 완전 연동, 로그아웃 시 즉시 로그인 화면 이동 |
| 냉장고 재료 관리 | 검색/영수증 OCR/AI 사진 인식(GPT-4o-mini Vision) 등록 |
| 레시피 추천 | 냉장고 재료 매칭률 계산 + 유통기한 임박 우선순위 |
| **레시피 카탈로그** | **총 95종** (한식 46 · 양식 20 · 중식 12 · 일식 11 · 분식 6) — 아래 참고 |
| 요리 선택 → 재료 소진 | 레시피 상세 하단 스티키 바에서 인분 조절 후 냉장고 재료 자동 차감 |
| **요리 배틀** | 초대 링크 비동기 대결 + 실시간 빠른 매칭, "내 배틀"/"전체 배틀" 탭, 관객 투표, 코멘트, 자동 타이머 — 아래 참고 |
| 게시판/요리 자랑 | 글쓰기, 좋아요, 실시간 댓글 알림, 공유 링크 다이얼로그 |
| 포인트/등급/랭킹 | 서버 트리거로 자동 적립, 실제 가입자 기준 랭킹 |
| 알림 | 댓글 실시간 + 유통기한 임박 로컬 + 배틀 이벤트 FCM 푸시 |
| 다국어 | 모든 화면 한/영 토글, 단위 자동 환산(g→oz 등) |
| 다크 테마 | 메인 대시보드만 다크 네이비, 나머지는 라이트 |

## 이번 세션 주요 작업

### 1. 요리 배틀 시스템 대개편
- **리스트 화면**: "내 배틀"/"전체 배틀" 탭 분리 — 전체 배틀은 참여 여부와 무관하게 진행 중인 모든 배틀 노출
- **상호작용 재설계**: 카드를 누르면(참여 가능 시) 바로 참여, 사람 이름을 누르면 바로 그 사람에게 투표 (숨어있던 버튼 방식 폐기)
- **투표 정책 변경**: 매칭 성사 즉시(양쪽 사진 제출 전이라도) 투표 가능, 종료 전까지 계속 가능, 본인 제외 누구나 가능
- **타이머 정책**: 매칭 대기 1일 초과 시 자동 **삭제**(취소 아님) / 양쪽 사진 제출 시 2일 카운트다운 시작 후 자동 마감 / 종료된 배틀은 최근 10건만 보관 후 자동 정리 — 전부 `close-expired-battles` Edge Function(5분마다 pg_cron)에 구현 후 프로덕션 배포 완료
- **사진+코멘트**: 배틀 사진 제출 시 200자 이내 짧은 한마디 함께 등록 가능 (`battle_participants.comment` 컬럼 라이브 DB에 추가)
- **버그 수정**: 사진 제출 후 코멘트 다이얼로그가 뜰 때 Flutter 프레임워크의 `_dependents.isEmpty` assertion으로 빨간 에러 화면이 뜨던 문제 (autofocus 제거 + 화면 전환 후 딜레이로 해결)
- 대시보드 실시간 배틀 티커: "⚔️ 배틀 진행중" 문구로 개선, 탭하면 배틀 메뉴로 이동

### 2. 레시피 대규모 확장 (원래 목표 50종 → **95종**으로 초과 달성)
- 김치찌개·제육볶음·된장찌개 등 대중적인 **"황금비율"** 시리즈로 파라프레이징 (특정 방송인 이름 언급 없이, 조리법 문구는 전부 앱 톤으로 새로 작성)
- 사진은 전부 **위키미디어 커먼즈 실사진**을 직접 검색·라이선스(CC BY/CC BY-SA/CC0/KOGL) 확인 후 사용 — URL 추측 없음
- 카테고리별로 배치 진행: 한식 대표 10종 → 한식 추가 10+10종 → 중식 10종(짜장면 등) → 양식 10종(까르보나라 등) → 일식 10종(규동 등) → 방송/유튜브 기법 영감 8종
- 신규 재료 전체를 `recipe_i18n.dart` 한/영 사전에 백필 완료 (매칭 로직에는 영향 없음 — 매칭은 항상 한글 원본명 기준)

### 3. 그 외 UI/UX 수정
- 메인화면 "영수증만 찍으면..." 배너 → 탭하면 재료 등록(영수증 스캔 탭)으로 이동하도록 연결
- My Menu "SNS공유" 라벨명 수정 (잘못된 위치 수정)
- 로그아웃 버튼을 마이 화면 하단에 눈에 띄게 추가 (기존 AppBar 아이콘은 잘 안 보였음)
- 레시피 상세 화면: 상단 숨은 알약 버튼 → 하단 스티키 바(인분 스테퍼 + "요리 선택하기") 로 교체
- 배틀 만들기 시트에서 레시피 선택 시 제목 필드가 숨어버리던 버그 수정

## 보류 중인 작업 — 관리자 모드
사용자 요청으로 **나중으로 미룸** (메모리에 기록됨: `fridge_chef_admin_mode_deferred`). "잔여 작업 있냐"는 질문을 받으면 먼저 물어보고 진행할 것.

- **배너 편집 관리자 화면**: 배너가 현재 `main_dashboard_screen.dart`/`banner_detail_screen.dart`에 하드코딩(DB 테이블 없음) — 우선순위 낮음
- **게시판 글 삭제**: `board_posts` 테이블에 DELETE RLS 정책이 아예 없음(작성자 본인도 삭제 불가) — **셋 중 가장 시급**
- **계정 관리**: 범위가 "정지/삭제/조회 중 뭘 원하는지"에 따라 크게 달라짐 — 사용자 확인 필요
- 공통 전제: `users.is_admin` 같은 권한 컬럼이 아예 없음. 세 기능 모두 RLS/SECURITY DEFINER로 서버 단에서 검증해야 함(클라이언트 UI 숨김만으로는 보안 구멍)

## 알려진 이슈 / 주의사항
- 개발용 Android 에뮬레이터(Pixel_7 AVD)는 카메라/갤러리 피커가 잘 응답하지 않고, 재부팅 직후 이미지 네트워크 로딩이 일시적으로 불안정할 때가 있음 — 코드 문제 아님, 실기기 확인 권장
- `flutter_image_compress_common` 플러그인이 Kotlin Gradle Plugin(KGP) 관련 경고를 매 빌드마다 출력함 (향후 Flutter 버전에서 빌드 실패 가능성 있다는 경고, 아직은 정상 빌드됨)
- 코드 변경 후에는 **반드시 `flutter build apk --debug` → 재설치 → 에뮬레이터 확인**까지 마쳐야 함 (이전에 코드만 커밋하고 재빌드를 안 해서 "적용이 안 됐다"는 혼선이 여러 번 있었음)

## 다음에 이어갈 만한 작업 후보
1. 관리자 모드 (게시판 삭제 → 계정 관리 → 배너 CMS 순으로 우선순위 제안했었음)
2. 레시피 95종에 대한 실기기 사진 로딩 최종 확인 (에뮬레이터 네트워크 이슈로 일부 배치 미확인)
3. 배틀 투표 기능의 실사용자 2인 시나리오 라이브 테스트 (지금까지는 코드 검증 위주)
